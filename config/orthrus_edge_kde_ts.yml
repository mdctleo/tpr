# Orthrus with edge features and KDE time encoding using raw timestamps
# Based on orthrus_non_snooped_edge_ts.yml with KDE parameters added

_include_yml: orthrus_non_snooped_edge_ts

# KDE-specific parameters for raw timestamp encoding
# Note: Uses fallback (base Orthrus encoder) for edges with < 10 timestamps
# KDE is only used for edges with >= 10 timestamps
kde_params:
  rkhs_dim: 20
  min_occurrences: 10
  bandwidth: 'scott'
  n_quadrature_points: 10
  time_dim: 50
  kde_vectors_dir: kde_vectors  # directory containing precomputed {DATASET}_kde_vectors.pt files
  use_precomputed: true          # use offline precomputed RKHS vectors (via kde_computation.py)

# Batch timing parameters (for performance analysis of KDE-eligible edges)
# This enables instrumentation to measure timing impact of tainted batches
training:
  enable_batch_timing: true      # Set to true to enable batch timing instrumentation
